# Playwright E2E Tests Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `@playwright/test` を導入し、ダッシュボード画面のタイトル・見出し・カード枚数・ラベル・色分けを検証する6つのe2eテストを追加する。

**Architecture:** Playwright の `webServer` 設定で `next start`（本番ビルド）を自動起動してテストを実行する。テスト前に `npm run build` が必要。ブラウザは Chromium のみ。

**Tech Stack:** @playwright/test, Next.js 16 (next start), TypeScript, Chromium

---

## File Map

| パス | 役割 |
|---|---|
| `playwright.config.ts` | Playwright 設定（testDir, baseURL, webServer, browser） |
| `e2e/dashboard.spec.ts` | ダッシュボード画面の e2e テストスイート |
| `package.json` | `@playwright/test` devDependency と `test:e2e` スクリプト追加 |

---

### Task 1: Playwright のインストールと設定

**Files:**
- Modify: `package.json`
- Create: `playwright.config.ts`

- [ ] **Step 1: @playwright/test をインストールする**

```bash
cd C:/Users/USER/repo/sample_coding
npm install --save-dev @playwright/test
```

期待される出力: `added N packages` のような npm インストール完了メッセージ。

- [ ] **Step 2: Chromium ブラウザバイナリをインストールする**

```bash
npx playwright install chromium
```

期待される出力: `Downloading Chromium ...` のような進捗メッセージと完了通知。

- [ ] **Step 3: playwright.config.ts を作成する**

プロジェクトルート（`C:/Users/USER/repo/sample_coding/`）に `playwright.config.ts` を作成:

```ts
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  use: {
    baseURL: 'http://localhost:3000',
  },
  projects: [
    { name: 'chromium', use: { browserName: 'chromium' } },
  ],
  webServer: {
    command: 'npm run start',
    port: 3000,
    reuseExistingServer: !process.env.CI,
  },
});
```

- [ ] **Step 4: package.json に test:e2e スクリプトを追加する**

`package.json` の `scripts` セクションを以下に更新する（既存のスクリプトはそのまま残し、`test:e2e` を追加）:

```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "lint": "eslint",
  "test:e2e": "playwright test"
},
```

- [ ] **Step 5: 設定ファイルが認識されることを確認する**

```bash
cd C:/Users/USER/repo/sample_coding
npx playwright --version
```

期待される出力: `Version X.X.X` のようなバージョン番号。

- [ ] **Step 6: コミット**

```bash
git -c user.email="user@example.com" -c user.name="User" add package.json package-lock.json playwright.config.ts
git -c user.email="user@example.com" -c user.name="User" commit -m "chore: add Playwright and configure e2e test setup"
```

---

### Task 2: ダッシュボード e2e テストの作成と検証

**Files:**
- Create: `e2e/dashboard.spec.ts`

**前提知識（セレクター根拠）:**
- カード要素: `MetricCard.tsx` の外側 `<div>` が `rounded-2xl` クラスを持つ。ページ上でこのクラスを持つ要素は4枚のカードのみ。
- 色分け: `change` 値が `+` で始まる場合は `text-green-600`（3件: +12%, +5%, +8%）、`-` で始まる場合は `text-red-500`（1件: -3%）。

- [ ] **Step 1: e2e ディレクトリと dashboard.spec.ts を作成する**

`e2e/dashboard.spec.ts` を作成:

```ts
import { test, expect } from '@playwright/test';

test.describe('Dashboard page', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
  });

  test('page title is ダッシュボード', async ({ page }) => {
    await expect(page).toHaveTitle('ダッシュボード');
  });

  test('h1 shows ダッシュボード', async ({ page }) => {
    await expect(page.locator('h1')).toHaveText('ダッシュボード');
  });

  test('shows 4 metric cards', async ({ page }) => {
    await expect(page.locator('.rounded-2xl')).toHaveCount(4);
  });

  test('shows all metric labels', async ({ page }) => {
    await expect(page.getByText('総売上')).toBeVisible();
    await expect(page.getByText('ユーザー数')).toBeVisible();
    await expect(page.getByText('完了タスク')).toBeVisible();
    await expect(page.getByText('新規注文')).toBeVisible();
  });

  test('positive changes have green color class', async ({ page }) => {
    await expect(page.locator('.text-green-600')).toHaveCount(3);
  });

  test('negative changes have red color class', async ({ page }) => {
    await expect(page.locator('.text-red-500')).toHaveCount(1);
  });
});
```

- [ ] **Step 2: アプリをビルドする（next start の前提条件）**

```bash
cd C:/Users/USER/repo/sample_coding
npm run build
```

期待される出力:
```
✓ Compiled successfully
✓ Generating static pages (4/4)
```

- [ ] **Step 3: テストを実行して全件パスすることを確認する**

```bash
cd C:/Users/USER/repo/sample_coding
npm run test:e2e
```

期待される出力:
```
Running 6 tests using 1 worker

  ✓ Dashboard page › page title is ダッシュボード
  ✓ Dashboard page › h1 shows ダッシュボード
  ✓ Dashboard page › shows 4 metric cards
  ✓ Dashboard page › shows all metric labels
  ✓ Dashboard page › positive changes have green color class
  ✓ Dashboard page › negative changes have red color class

  6 passed (Xs)
```

6件全件パスすることを確認する。失敗した場合は Step 4 へ進む前にデバッグすること。

- [ ] **Step 4: コミット**

```bash
git -c user.email="user@example.com" -c user.name="User" add e2e/dashboard.spec.ts
git -c user.email="user@example.com" -c user.name="User" commit -m "test: add Playwright e2e tests for dashboard page"
```
